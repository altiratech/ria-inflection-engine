# API App

Intended responsibilities:
- source ingest helpers
- normalization and diff orchestration
- scoring endpoints or job triggers if the build needs a thin runtime layer

Do not start here with broad service architecture.
Start from schemas and ingest truth.
